$("#process").load("<%=escape_javascript(processing_url)%>");
$("#failures").load("<%=escape_javascript(failures_url)%>");
$("#content_<%=@content.id%>").fadeTo(400, 0, function() {
  $(this).remove();
  $('.block table tr:odd').css('background-color', '#fbfbfb');
});